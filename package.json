{
	"name": "x402-pay",
	"version": "0.1.2",
	"private": true,
	"description": "Build pipeline for the x402 Pay admin UI.",
	"scripts": {
		"build": "wp-scripts build --webpack-src-dir=assets/src --output-path=assets/build",
		"start": "wp-scripts start --webpack-src-dir=assets/src --output-path=assets/build",
		"package": "scripts/package.sh"
	},
	"devDependencies": {
		"@wordpress/scripts": "^30.0.0"
	},
	"dependencies": {
		"@wordpress/dataviews": "^14.1.0"
	}
}
